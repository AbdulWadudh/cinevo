"use client";

// The site navigation is now the StaggeredMenu overlay (see SiteMenu).
// Re-exported as Nav so existing `<Nav />` usages across pages keep working.
export { default } from "@/components/SiteMenu";
